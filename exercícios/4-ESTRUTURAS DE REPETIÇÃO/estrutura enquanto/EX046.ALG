algoritmo "EX046"
var
   cont,s:inteiro

inicio
      cont <- 6
      s <- 0
      enquanto (cont <=100) faca
               escreval(cont)
               s <- s + cont
               cont <- cont +2

      fimenquanto
      escreval("A soma desses números é ",s)

fimalgoritmo